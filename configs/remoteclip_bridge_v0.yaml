experiment:
  name: remoteclip_bridge_v0
  seed: 42
  overwrite: false

paths:
  pixel_pack: inputs/encoder_compare_pack_v0/run_20260813_001/pixel_pack
  checkpoint: inputs/checkpoints/RemoteCLIP-ViT-B-32.pt
  protocol_file: configs/remoteclip_bridge_protocol_v0.json
  output_root: outputs/remoteclip_bridge_v0

model:
  architecture: ViT-B-32
  feature_dim: 512
  device: auto
  batch_regions: 64
  normalize_features: true

